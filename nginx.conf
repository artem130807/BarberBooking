events {
    worker_connections 1024;
}

http {
    upstream barber_backend {
        least_conn;                    
        server prod_api1:8080 max_fails=3 fail_timeout=30s;
        server prod_api2:8080 max_fails=3 fail_timeout=30s;
        server prod_api3:8080 max_fails=3 fail_timeout=30s;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://barber_backend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}